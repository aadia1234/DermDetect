import SwiftUI
import AVKit


struct ContentView: View {
    // Please build this app with Xcode 14 on macOS 13, and run it on an iPad with iPadOS 16
    var body: some View {
        NavigationStack {
            WelcomeView()
        }
    }
}
