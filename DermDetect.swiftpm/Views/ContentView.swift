import SwiftUI
import AVKit


struct ContentView: View {
    //Please build and run this app on Swift Playgrounds on an iPad with iPadOS 16
    var body: some View {
        NavigationStack {
            WelcomeView()
        }
    }
}
